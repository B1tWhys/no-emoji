# No-emoji

I hate how LLMs insist on putting non-ascii characters in comments and strings.
This is a super simple pre-commit hook (written in rust for *speed*) that rejects
changes containing non-unicode characters.

## Installation
TODO
